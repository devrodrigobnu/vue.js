const req = require('prompt-sync')
const prompt = req()

// Faca um programa que imprima os números de 1 a 10 usando um loop while.

// interator
let i = 0

while(i < 10) { // enquanto o valor da variável i for menor que 10
    i++ //i recebe + 1
    console.log(i)
}